<div class="container-xxl position-relative p-0">
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark px-4 px-lg-5 py-3 py-lg-0">
    <a href="/" class="navbar-brand p-0">
      <h1 class="text-primary m-0">
        <i class="fa fa-gem"></i>
        DORIAN
      </h1>
    </a>

    <div class="navbar-nav ms-auto py-0 pe-4">
      <% if user_signed_in? %>

        <%= link_to "Products", products_path, class: "nav-item nav-link" %>
        <% if current_user.orders.where(status: 'pending').last.present? %>
          <li class="nav-item">
            <%= link_to "My cart", "/orders/#{current_user.orders.last.id}", class: "nav-link" %>
          </li>
        <% end %>
        <% if current_user.admin? %>
          <li class="nav-item">
            <%= link_to "Add product", "/products/new", class: "nav-link" %>
          </li>
        <% end %>
          <li class="nav-item">
            <%= link_to "My orders", "/users/#{current_user.id}/orders", class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Log out", destroy_user_session_path, :method => :delete, class: "nav-link" %>
          </li>
          </div>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to "Login", new_user_session_path, class: "nav-link" %>
        </li>
    <% end %>
  </nav>
</div>